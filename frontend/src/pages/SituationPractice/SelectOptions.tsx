function SelectOptions(){
    return(
        <div>대본등록, 불러오기 둘 중 하나 선택..</div>
        // 대본 등록 시 -> /situation/practice/regist/script (RegistScript)
        // 대본 불러올떄 -> /situation/practice/select/script (SelectScript)
    )
}

export default SelectOptions;